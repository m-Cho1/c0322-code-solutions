/* exported getValue */

// input: two arguments (object, key):
//          object: any js object
//          key: a js string
// output: value of object at the key property

// access value of key property in object
// return the value as a return value

function getValue(object, key) {
  return object[key];
}
