/* exported last */

// input: any js array
// output: the last element of the given array.

// - access the last element of the array, using length property.
// - return the last element of the array as a return value.

function last(array) {
  return array[array.length - 1];
}
