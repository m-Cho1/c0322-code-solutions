/* exported head */

// input: any js array
// output: the first element of the array

// - access the first element in the array.
// - first element in the array is index of 0.
// - return the first element as a return value.

// function head(array) {
//   return array.at(0);
// }

function head(array) {
  return array[0];
}
